## ginkgo-cli quic serve

Start a minimal QUIC server

```
ginkgo-cli quic serve [flags]
```

### Options

```
      --acme-ca string           ACME directory URL (optional; defaults to Let's Encrypt prod)
      --addr string              listen address (host:port) (default ":7845")
      --alpn-port int            alternate port for TLS-ALPN-01 challenge (default 443)
      --cert string              path to TLS certificate (PEM)
      --domain string            domain for ACME (CertMagic)
      --eab-hmac string          ACME External Account Binding HMAC key (optional)
      --eab-key-id string        ACME External Account Binding key ID (optional)
      --email string             email for ACME registration
      --enable-tls-alpn          enable ACME TLS-ALPN-01 challenge (requires TCP :443 or --alpn-port)
  -h, --help                     help for serve
      --http-addr string         HTTP address for ACME HTTP-01 challenge (default ":80")
      --insecure-self-signed     use an ephemeral self-signed cert (NOT recommended)
      --key string               path to TLS private key (PEM)
      --storage-dir string       CertMagic storage directory (optional)
      --zerossl-api-key string   Use ZeroSSL API issuer (bypass ACME) with this API key
```

### Options inherited from parent commands

```
      --config string   path to config file (yaml|toml)
```

### SEE ALSO

* [ginkgo-cli quic](ginkgo-cli_quic.md)	 - Experimental QUIC tools (server and ping)

###### Auto generated by spf13/cobra on 20-Jan-2026
