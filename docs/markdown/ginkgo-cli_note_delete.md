## ginkgo-cli note delete

Delete notes

```
ginkgo-cli note delete [id...] [flags]
```

### Options

```
      --dry               show what would be deleted without making changes
  -h, --help              help for delete
      --output string     dry-run output mode: plain|pretty|json|ndjson
  -s, --since string      Show notes created since a time (absolute: '2025-10-26T14:30', relative: '2h', '3d')
      --tags-all string   comma-separated tags; match notes containing all
      --tags-any string   comma-separated tags; match notes containing any
  -u, --until string      Show notes created until a time (absolute or relative; same formats as --since)
      --yes               skip confirmation prompt for bulk deletes
```

### Options inherited from parent commands

```
      --config string      path to config file (yaml|toml)
  -n, --namespace string   override namespace for this command
```

### SEE ALSO

* [ginkgo-cli note](ginkgo-cli_note.md)	 - Work with notes (default: add one-liner)

###### Auto generated by spf13/cobra on 8-Jan-2026
